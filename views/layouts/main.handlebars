
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Let Them Cook</title>
    <link rel="stylesheet" href="/css/jass.css">
</head>

<body>
    <header class="display-block justify-space-between align-center p-2">

        <nav>
            <button class="btn" id="home" onclick="(window.location='/')">Home</button>
            {{#if logged_in}}
            <button class="btn" id="logout">Logout</button>
            {{else}}
            <button class="btn" id="login" onclick="(window.location='/login')">Login</button>
            <button class="btn" id="createUser" onclick="(window.location='/newUser')">Create Account</button>
            {{/if}}
            
        </nav>

        <h1 class="ml-3">
            LEt them CooK
        </h1>

    </header>

    <main class="container container-fluid mt-5">
        {{{ body }}}
    </main>

    <footer>

    </footer>

    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}

</body>

</html>
